{{ meta.alog }}+{{ meta.blog }}	Controller Provisioned A (median)	Controller Provisioned B (median)	Provisioning Runtime A (total)	Provisioning Runtime B (total)	Runtime/Instance A	Runtime/Instance B	PVC Deleted A (median)	PVC Deleted B (median)	Deletion Runtime A (total)	Deletion Runtime B (total)	Runtime/Instance A	Runtime/Instance B
{% for _ in a %}
{{ a[loop.index0].benk.metadata.parameters.config.workloadInstances }}	{{ a[loop.index0].benk.controllers.metadata.provisioned.median  }}	{{ b[loop.index0].benk.controllers.metadata.provisioned.median }}	{{ a[loop.index0].benk.controllers.metadata.provisioned.runtime }}	{{ b[loop.index0].benk.controllers.metadata.provisioned.runtime }}	{{ a[loop.index0].benk.controllers.metadata.provisioned.runtime|float / a[loop.index0].benk.metadata.parameters.config.workloadInstances|float }}	{{ b[loop.index0].benk.controllers.metadata.provisioned.runtime|float / b[loop.index0].benk.metadata.parameters.config.workloadInstances|float }}	{{ a[loop.index0].benk.pvcs.metadata.destruction.median  }}	{{ b[loop.index0].benk.pvcs.metadata.destruction.median }}	{{ a[loop.index0].benk.pvcs.metadata.destruction.runtime }}	{{ b[loop.index0].benk.pvcs.metadata.destruction.runtime }}	{{ a[loop.index0].benk.pvcs.metadata.destruction.runtime|float / a[loop.index0].benk.metadata.parameters.config.workloadInstances|float }}	{{ b[loop.index0].benk.pvcs.metadata.destruction.runtime|float / b[loop.index0].benk.metadata.parameters.config.workloadInstances|float }}
{% endfor %}
